<% pocs.each do | poc| %>
  <tr class="cursor" data-link="<%= point_of_contact_path(poc)%>">
    <td><%= poc.first_name %></td>
    <td><%= poc.last_name %></td>
    <td><%= poc.email %></td>
  </tr>
<% end %>